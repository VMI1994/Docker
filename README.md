```git clone https://github.com/VMI1994/Docker && cd Docker && bash Install.sh```

A bash script to install docker and the dockhand container in linux.  Options for docker cli for servers and docker desktop for linux X64 systems.
